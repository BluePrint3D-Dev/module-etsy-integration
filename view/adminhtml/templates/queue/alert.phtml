<!-- BLUEPRINT3D ERROR BANNER -->
<?php $errorCount = $block->getErrorCount(); ?>
<?php if ($errorCount > 0): ?>
    <?php $errorDetails = $block->getErrorDetails(); ?>
    <div style="background: linear-gradient(90deg, #fff5f5 0%, #ffebeb 100%);
                border-left: 6px solid #e22626; padding: 16px 24px; margin-bottom: 20px;
                box-shadow: 0 2px 6px rgba(0,0,0,0.06); display: flex; align-items: flex-start;
                justify-content: space-between; font-family: 'Open Sans', sans-serif; border-radius: 2px;">

        <div style="display: flex; align-items: flex-start;">
            <div style="font-size: 26px; margin-right: 18px; line-height: 1; padding-top: 4px;">⚠️</div>
            <div>
                <h4 style="margin: 0 0 6px 0; color: #cc0000; font-size: 16px; font-weight: 700;
                           text-transform: uppercase; letter-spacing: 0.5px;">
                    <?= $escaper->escapeHtml(__('Etsy Sync Error Detected')) ?>
                </h4>
                <p style="margin: 0 0 10px 0; color: #555; font-size: 14px; line-height: 1.4;">
                    <strong><?= (int)$errorCount ?> <?= $escaper->escapeHtml(__('product(s)')) ?></strong>
                    <?= $escaper->escapeHtml(__('failed to sync to Etsy. Please review the errors below:')) ?>
                </p>

                <?php if (!empty($errorDetails)): ?>
                    <ul style="margin: 0; padding: 0 0 0 18px; color: #cc0000; font-size: 13px;
                               font-weight: 600; max-height: 150px; overflow-y: auto;">
                        <?php foreach ($errorDetails as $error): ?>
                            <li style="margin-bottom: 4px;">
                                <strong><?= $escaper->escapeHtml($error['sku']) ?>:</strong>
                                <span style="font-weight: 400; color: #333;">
                                    <?= $escaper->escapeHtml($error['message']) ?>
                                </span>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php endif; ?>
            </div>
        </div>

        <!-- THE NEW RETRY BUTTON -->
        <div style="margin-left: 20px; flex-shrink: 0; padding-top: 4px;">
            <a href="<?= $escaper->escapeUrl($block->getRetryUrl()) ?>"
               style="background-color: #e22626; color: #fff; padding: 12px 24px;
                      text-decoration: none; font-weight: 600; border-radius: 4px;
                      display: inline-block; text-transform: uppercase; font-size: 13px;
                      letter-spacing: 0.5px; box-shadow: 0 2px 4px rgba(226,38,38,0.3);
                      transition: background-color 0.2s ease;">
                <?= $escaper->escapeHtml(__('Retry Failed Syncs')) ?>
            </a>
        </div>

    </div>
<?php endif; ?>